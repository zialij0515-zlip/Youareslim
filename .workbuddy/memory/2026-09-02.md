# 2026-09-02 工作日志

## Git 仓库初始化（微信小程序 Youareslim）
- 工作区原本不是 git 仓库；残留一个属主为 `CodexSandboxOffline` 的损坏 `.git`（dubious ownership），已删除并以当前用户 `Zia` 重新 `git init`。
- 修正了全局 git 身份写入错误：原本 `user.name='zia` / `user.email='zialij0515@gmail.com`（开头多了单引号），已改为 `zia` / `zialij0515@gmail.com`（写在 `C:\Users\Zia\.gitconfig`）。
- 新增 `.gitignore`：忽略 `project.private.config.json`、node_modules、miniprogram_npm、dist、日志、系统临时文件。
- 首次提交 `c2e47d0`（分支 `master`），共 96 个文件；`project.private.config.json` 已正确排除。
- 远程仓库已完成：GitHub 账号 `zialij0515-zlip`，目标仓库 `Youareslim`（Public，原本已建好空仓库，size=0，故直接复用，无需新建）。
- 本地分支 `master` 改名为 `main`（对齐 GitHub 默认分支）；`git remote add origin https://github.com/zialij0515-zlip/Youareslim.git`。
- 用用户提供的 PAT（ghp_***）临时鉴权完成首次推送：`git push -u origin main` 成功，`main` 已跟踪 `origin/main`，远程已确认收到提交 `c2e47d0`。
- 令牌仅用于本次推送命令，未写入 `.git/config`（remote -v 为纯净地址）。
- 后续日常提交流程：`git add -A` → `git commit -m "..."` → `git push`（本机已登录 GitHub 时直接 push；沙箱内则需再次提供 PAT 或用 Git Credential Manager）。

## 减脂打卡小程序 - Phase A（云环境接通 + 集合 + 权限）代码落地
- 用户确认：云环境 ID = `cloud1-d1g0on3869c3170d5`；数据隔离方案 = 客户端直写 + 按 openid 权限隔离（默认方案）。
- `utils/cloud.js`：填入 ENV_ID；新增幂等 `init()`、`getOpenid()`（调用 login 云函数取 openid 并缓存）；`uploadImages()` 路径改为按用户隔离 `checkins/{openid}/{date}_{ts}_{idx}.jpg`；保留 `syncCheckin`/`syncBodyRecord`。
- `app.js`：移除硬编码 env，改用 `cloud.init()` 单一来源。
- 新建云函数 `cloud1-d1g0on3869c3170d5/login`（返回 openid）与 `cloud1-d1g0on3869c3170d5/init`（批量建 5 集合：users/body_records/checkins/daily_summary/posters）。
- 重写 `docs/cloud-development-setup.md`：含 env ID、云函数部署步骤、5 个集合的权限规则 JSON（read/write = doc._openid == auth.openid）、云存储权限设置、真机验收清单。
- ⏳ 等待用户在微信开发者工具里：部署 login+init 云函数并运行 init 建集合、给 5 个集合粘贴权限规则、云存储设为仅创建者可读写。沙箱无法点控制台，这步需用户执行。
- 语法校验全部通过（node --check）。本地已提交检查点（未推送）。

## Phase A 修复：云函数同名冲突
- 用户部署 `login` 云函数时报 `ResourceInUse.FunctionName`：云环境 `cloud1-d1g0on3869c3170d5` 已自带默认 `login`（快速启动模板），同名无法重复创建。
- 规避方案：将 `login` → `slim-login`、`init` → `slim-init`（不覆盖用户已有函数）。同步更新：`utils/cloud.js` 的 `callFunction({name:'slim-login'})`、两个 package.json 的 name、以及 `docs/cloud-development-setup.md` 全部引用。
- 已 git 提交 `c053360`，grep 确认无旧 `login`/`init` 路径残留（不含 cloud.init()）。
- 用户下一步：在开发者工具对 `slim-login`、`slim-init` 右键上传并部署，运行一次 `slim-init` 建集合。

## Phase A 收尾（用户完成控制台操作）
- 用户已在微信开发者工具部署 `slim-login`/`slim-init`、运行 `slim-init` 建好 5 个集合、并给集合+云存储配了按 openid 权限。Phase A（云环境接通+集合+权限）彻底完成。

## Phase B：建档 + 首页（代码实现，已提交 16e8ff3）
- 用户确认决策：Banner 用「左右滑动卡片」（微信原生 swiper + indicator-dots）；可爱插画「先用代码占位」（imagegen 留到后续批量出）。
- `utils/cloud.js`：新增 `syncProfile`（建档写 users 集合，按 openid 作 doc id 幂等 set）、`getLatestBodyRecord`、`getTodayCheckins`（云端拉取，本地缺失时跨设备恢复；用 .get() 后在 JS 排序/过滤，规避云数据库索引要求）。
- `pages/onboarding`：建档提交时本地保存 + 云端同步（syncProfile + syncBodyRecord）；wxml 补「目标日期（可选）」字段（type=date）。昵称字段原本已有。
- `pages/home`：用 `swiper` 实现 4 张可滑动 Banner（目标进度/数据趋势/今日鼓励/成就）；进度环改为 conic-gradient 甜甜圈（按 progress% 填充，中心显示百分比）；新增连续打卡天数 computeStreak、近期体重变化 recentChange、按完成度轮换的鼓励语；onShow 优先本地、best-effort 拉云端合并（失败不阻塞）。
- 视觉沿用浅绿 Morandi + 圆角卡片，未引入 imagegen 插画。
- 语法校验全过（node --check）。本地提交 16e8ff3。
- ⏳ 待用户在开发者工具真机/模拟器预览 Phase B 效果；记录页 `activeType` 预选逻辑待 Phase D 消费。

## Phase B 预览问题修复：WXML 非法标签 + 云开发开关
- 用户按 `Ctrl+B` 编译报错：`./pages/data/data.wxml:1:272 expect end-tag 'text', near 'view'`。
- 根因：多个 wxml 页面混用了 HTML 标签 `<b>`、`<small>`，微信小程序 WXML 解析器不认识；且 `<text>` 内不能嵌套 `<view>` 等块级元素。
- 修复：将 `pages/data/data.wxml`、`pages/profile/profile.wxml`、`pages/poster/poster.wxml` 中的 `<b>` / `<small>` 全部替换为 `<text class="...">`，并用标准 WXML 结构重写。
- 云环境选择器未出现：根因是 `app.json` 缺少 `"cloud": true`，开发者工具未识别为云开发项目。已补上该字段。
- JS 语法校验无 HTML 标签残留，本地提交 2635472。
