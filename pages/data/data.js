const { getProfile, getBodyRecords, getAllCheckins } = require('../../utils/store')
const { labelForBmi } = require('../../utils/metrics')
function chart(records) { const values=records.map(r=>Number(r.weight)).filter(Number.isFinite); if (!values.length) return []; const min=Math.min(...values), max=Math.max(...values), span=max-min||1; return records.map((item,index)=>({ ...item, left: records.length===1?50:Math.round(index/(records.length-1)*92+4), bottom:Math.round((Number(item.weight)-min)/span*68+12) })) }
Page({ data:{ profile:{}, latest:{}, records:[], points:[], completion:0, note:'' }, onShow(){ const profile=getProfile(), records=getBodyRecords(), latest=records[records.length-1]||{}; const all=getAllCheckins(); const days=new Set(all.map(i=>i.date)).size; const completion=days?Math.round(all.length/(days*5)*100):0; const note=latest.weight ? `当前 BMI ${latest.bmi}，${labelForBmi(latest.bmi)}。变化不必每天都线性，持续记录更重要。` : '先记录一次体重，就能从这里看到属于你的趋势。'; this.setData({profile,latest,records:records.slice(-7),points:chart(records.slice(-7)),completion,note}) } })
